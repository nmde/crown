import { VNode } from 'vue';
import { component } from 'vue-tsx-support';

/**
 * Login/signup page
 */
export default component({
  render(): VNode {
    return (
      <h1>Login Page</h1>
    );
  },
});
